name: Cocoapods

on:
  push:
    tags:
      - 'ios-*'

jobs:
  Deploy:
    name: Publish
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Lint
        run: |
          pod spec lint
      - name: Push
        run: |
          pod trunk push
